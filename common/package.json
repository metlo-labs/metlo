{
  "name": "@metlo/common",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "postbuild": "mkdir -p dist/ && cp -R proto dist",
    "watch": "tsc -w",
    "format": "prettier --write './src/**/*.{ts,tsx}'"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@metlo/testing": "^0.0.2",
    "@types/axios": "^0.14.0",
    "axios": "^0.27.2"
  }
}