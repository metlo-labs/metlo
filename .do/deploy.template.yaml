spec:
  name: sample-golang
  services:
    - envs:
        http_port: 80
        image:
          registry: okteto
          registry_type: DOCKER_HUB
          respository: hello-world
          tag: latest
