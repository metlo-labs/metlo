spec:
  name: sample-metlo
  region: blr
  services:
  - http_port: 8080
    image:
      registry: metlo
      registry_type: DOCKER_HUB
      repository: backend
      tag: latest
    instance_count: 1
    instance_size_slug: basic-xxs
    name: metlo-backend
    routes:
    - path: /
